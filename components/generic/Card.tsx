import React from "react";
import styles from "styles/components/generic/Card.module.scss";

export default function Card() {
    return (
        <div className={styles.card}>
            Hello, World!
        </div>
    );
}